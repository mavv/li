export default class Image {
	constructor (it) {
		this.originalContextUrl = it.originalContextUrl;
		this.publisher = it.publisher;
		this.tbWidth = it.tbWidth;
		this.tbHeight = it.tbHeight;
		this.tbUrl = it.tbUrl;
		this.url = it.url;
	}
}
